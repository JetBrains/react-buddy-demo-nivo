import React from 'react';
import {Category, Palette,} from '@react-buddy/ide-toolbox';

export const PaletteTree = () => (
    <Palette>
        <Category name="Charts(Nivo)">

        </Category>
    </Palette>
);